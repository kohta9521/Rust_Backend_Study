fn main() {
    // ベクタ型
    // 配列とはことなり要素数を変更できる
    // let v1: Vec!<i32> = Vec![1, 2, 3];
    let mut v3: Vec<unknown> = Vec::new();

    v3.push(1);
    v3.push(2);
    v3.push(3);

    println!("v1[0] is {}", v1[0]);
}